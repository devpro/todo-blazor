name: PKG
# description: Continuous Delivery (aka Packaging & Shipping)

on:
  push:
    branches: [main]
  workflow_dispatch: {}

jobs:
  git-check:
    name: Git
    uses: ./.github/workflows/reusable-git-check.yml

  image-publication:
    name: Image
    needs: git-check
    if: github.event_name == 'workflow_dispatch' || needs.git-check.outputs.app_changed == 'true'
    permissions:
      id-token: write
      contents: read
    uses: devpro/github-workflow-parts/.github/workflows/reusable-container-publication.yml@f61750746227ea28ae379e5a36ad52096fb34967
    with:
      create-latest: ${{ github.ref_name == 'main' }}
      extra-build-arguments: >-
        --secret id=additional_file_name,env=ADDITIONAL_FILE_NAME
        --secret id=additional_file_content,env=ADDITIONAL_FILE_CONTENT
      image-definition: "src/BlazorApp/Dockerfile"
      image-name: ${{ vars.CONTAINER_IMAGE_NAME }}
      image-path: ${{ vars.CONTAINER_REGISTRY_PATH }}
      image-tag: "${{ needs.git-check.outputs.version_major_minor }}.${{ github.run_id }}"
      workflow-parts-version: f61750746227ea28ae379e5a36ad52096fb34967
    secrets:
      container-registry-password: ${{ secrets.DOCKERHUB_TOKEN }}
      container-registry-username: ${{ secrets.DOCKERHUB_USERNAME }}
      extra-vars: |
        ADDITIONAL_FILE_NAME=${{ secrets.CONTAINER_ADDITIONAL_FILENAME }}
        ADDITIONAL_FILE_CONTENT=${{ secrets.CONTAINER_ADDITIONAL_FILECONTENT }}
